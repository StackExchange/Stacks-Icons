*.DS_Store
.AppleDouble
.LSOverride
.metadata_never_index

# Resource forks
._*

node_modules
dist/
preview/

# files we download from figma in src/fetchFigmaComponents
src/Icon
src/Spot

# don't check in environmental variables / secrets
.env

# temporary output from sync:figma, used for PR generation
.sync-output.json

# don't check in any c# build data
dotnet/**/obj
dotnet/**/bin
.vs